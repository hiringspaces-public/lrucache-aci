# code-server configuration
# Auth is disabled here — the HiringSpaces backend controls access
# via a session token passed in the ACI environment variables.
bind-addr: 0.0.0.0:8080
auth: none
password: ""
cert: false

# Open the workspace on start
# The workspace file is pre-configured with Java + C# settings
